Roadmap.md — מפת דרכים ורשימת משימות
​שלב 0: הגדרות ותשתית (הושלם ✅)
​[x] יצירת Project_Overview.md (מטרות וחוקי ברזל).
​[x] יצירת Specs_Master.md (אפיון לוגי, זרימות וזמנים).
​[x] יצירת Architecture.md (בחירת טכנולוגיות, מסדי נתונים ושכבות).
​[x] יצירת מבנה תיקיות ריפו (Scaffolding).
​שלב 1: MVP בסיסי - "Hello World" של המרקטפלייס (בביצוע 🚧)
​המטרה: בוט שמגיב, רושם משתמשים ומאפשר לפרסם ולקבל קריאה ברמה הבסיסית.
​[ ] חיבור ל-BotFather, השגת טוקן והקמת חיבור בסיסי ב-Python (Asyncio).
​[ ] הקמת בסיס הנתונים (PostgreSQL / SQLite) וטבלאות Users ו-Jobs.
​[ ] פיתוח זרימת "הרשמה לנהג" (שמירת טלפון, סוג רכב, אימות בסיסי).
​[ ] פיתוח זרימת "בקשת נסיעה/משלוח" (מיידית בלבד בשלב זה).
​[ ] שידור הבקשה ללוח כללי ואישור נהג ראשון (עם נעילת DB - Database Lock).
​שלב 2: The Time Engine - ניהול זמנים וקצוות (Next)
​המטרה: טיפול בעתיד, איחורים, ביטולים וציפיות.
​[ ] שילוב משימות עתידיות (Scheduled Jobs) עם תאריך ושעה.
​[ ] פיתוח מנגנון הצהרת הגעה (ETA) לנהג.
​[ ] הפעלת APScheduler לניהול התראות אוטומטיות (תזכורת לנהג 15 דקות לפני).
​[ ] פיתוח לוגיקת ביטולים (החזרת משימה לשוק אם נהג מאחר/מבטל).
​שלב 3: חסינות ופיקוח (Anti-Fragility)
​המטרה: המערכת מפקחת על המערכת ומונעת קריסות.
​[ ] חיבור Redis והפעלת Rate Limiter (חסימת משתמשים שמספימים את הבוט).
​[ ] הקמת סקריפט ה-Watchdog (Health Check שמאשר שהבוט רץ ומאתחל במקרה קריסה).
​[ ] מערכת לוגים ושליחת התראות Exception לערוץ Admin ייעודי בטלגרם.
​שלב 4: Scale ופוליש לממשק (Addictive UX)
​המטרה: הפיכת הבוט ל"ממכר" ויעיל יותר.
​[ ] מערכת דירוג 1-5 כוכבים לנהגים ולקוחות בסיום נסיעה.
​[ ] הוספת מנגנון "Share Location" מהיר לחילוץ קואורדינטות (במקום הקלדת עיר).
​[ ] ממשק אדמין לניהול משתמשים (Ban / Broadcast).
